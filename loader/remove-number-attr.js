module.exports = function (source) {
    console.log(source);

    // function removeNumberKey(obj) {
    //
    //     for (let key in obj) {
    //         if (obj.hasOwnProperty(key)) {
    //             if (Number(key)) {
    //                 delete obj[key];;
    //             }
    //         }
    //     }

    return source;
};
